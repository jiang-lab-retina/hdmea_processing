"""
High Glucose JHU Analysis Pipeline

Analyzes step responses during high-glucose perfusion experiments.
Each recording is processed independently (no cross-recording alignment).
Uses data_loader from the sibling step_change_analysis package.
"""
